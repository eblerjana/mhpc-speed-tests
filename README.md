# mHPC speed tests

Simple C++ program which reads a file line by line.

## Compilation

``  g++ main.cpp -o speedTest ``

### Compilation flags used for PanGenie:

``  g++ main.cpp -Wall -Wextra -Wno-long-long -Wno-unknown-pragmas -O3 -o speedTest ``

## How to run

`` ./speedTest <VCF-file> ``
